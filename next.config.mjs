/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Single-Page Application(SPA)로 출력합니다.
    distDir: './dist', // 빌드 결과물을 ./dist 디렉토리에 저장합니다.
};

export default nextConfig;
